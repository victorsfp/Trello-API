
class BoardModel {
    
    constructor(id, name, desc, descData, closed, dateClosed, idOrganization, url, shortLink){
        this.id = id
        this.name = name
        this.desc = desc
        this.descData = descData
        this.closed = closed
        this.dateClosed = dateClosed
        this.idOrganization = idOrganization
        this.url = url
        this.shortLink = shortLink
    }
}